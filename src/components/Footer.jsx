const Footer = () => {
  const footerStyle = {
    backgroundColor: 'var(--primary-color)',
    color: 'var(--text-on-primary)',
    padding: '1.5rem',
    textAlign: 'center',
    marginTop: 'auto' 
  };

  return <footer style={footerStyle}>© 2026 Brand Inc.</footer>;
};

export default Footer;